// src/services/userService.js

import axios from 'axios';

const API = '${import.meta.env.VITE_BACKEND_URL}/api/user'; // Update when backend is ready

export const fetchToolHistory = async (token) => {
  // Placeholder for future user API
  return [];
};