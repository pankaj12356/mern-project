// src/services/adminService.js

import axios from 'axios';

const API =  `${import.meta.env.VITE_BACKEND_URL}/api/admin`; // Update when backend is ready

export const fetchAllUsers = async (token) => {
  // Placeholder for future admin API
  return [];
};