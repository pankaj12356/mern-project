// src/services/adminService.js

import axios from 'axios';

const API = 'http://localhost:4000/api/admin'; // Update when backend is ready

export const fetchAllUsers = async (token) => {
  // Placeholder for future admin API
  return [];
};